/** Bearer authorization header prefix. */
const BEARER_PREFIX = "Bearer ";

/** Header name accepted by the HTTP authentication adapter. */
const AUTHORIZATION_HEADER = "authorization";

/** SHA-256 hexadecimal length used by the constant-time comparison loop. */
const SHA256_HEX_LENGTH = 64;

export { AUTHORIZATION_HEADER, BEARER_PREFIX, SHA256_HEX_LENGTH };
